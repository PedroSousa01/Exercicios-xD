algoritmo "semnome"

var
   S, C: Inteiro
inicio
      C <- 6
      S <- 0
      Enquanto (C <= 100) faca
         Escreva(C, " +")
         C <- C + 2
         S <- S + C
      FimEnquanto
      EscrevaL()
      Escreva("A soma entre esses números é igual a:", S)
fimalgoritmo
